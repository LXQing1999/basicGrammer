#include <cstdio>
#include <cstring>
using namespace std;

char *mycpy(char *des, const char *src)
{
    int idx = 0;
    while (scr[idx])
    {
        des[]
    }
}

int main(int argc, char const *argv[])
{
    char s1[] = "abc";
    char s2[] = "abc";
    strcpy(s1, s2);
    return 0;
}
